<!DOCTYPE html>
<html lang="en">
<%- include('layout/head') %>

<body>
    <%- include('layout/posts-header') %>
    <table>
        <tr>
            <td>id</td>
            <td>writer</td>
            <td>title</td>
            <td>createdAt</td>
        </tr>
        <% for( v of res) { %>
        <tr>
            <td><%= v.id %></td>
            <td><%= v.writer %></td>
            <td><a href="/posts/select/<%= v.id %>"><%= v.title %></a></td>
            <td><%= v.createdAt %></td>
        </tr>
        <% } %>
        <script>
        </script>
    </table>
</body>

</html>